{
  "name": "mobilis-blockchain",
  "version": "1.0.0",
  "description": "Local Hardhat sandbox for Mobilis Bitcoin Test page - NFT, DeFi, DAO, Supply Chain, Identity contracts",
  "scripts": {
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "compile": "hardhat compile"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "npm:@nomicfoundation/hardhat-toolbox@hh2",
    "hardhat": "^2.28.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
